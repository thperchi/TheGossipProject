<section class="jumbotron text-center">
    <div class="container">
      <h1><%= @user.first_name %> <%= @user.last_name %></h1>
      <p class="lead text-muted"><%= @user.age %> ans.<br/>
      Vit à <%= @user.city.name %>.<br/>
      <%= @user.description %><br/>
      <%= @user.email %></p>
    </div>
</section>
    <% @author_gossips.each do |gossip| %>
        <div class="container">
        <div class="row">
        <div class="col-md-4">
          <div class="card mb-4 shadow-sm">
            <div class="card-body">
              <h1 class="card-text"><%= gossip.title %></h1>
              <div class="d-flex justify-content-between align-items-center">
                <div class="btn-group">
                  <%= link_to 'Voir le potin', gossip_path(gossip.id), class: "btn btn-sm btn-outline-secondary" %>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
<% end %>